# SmartStudyPal 开发提示词日志

> 项目名称：基于视觉-语音多模态融合的智能陪伴自习系统
> 开发工具：Trae（AI IDE）+ Vibe Coding
> 开发者：陈岳 230141875
> 仓库地址：https://github.com/Yue230141875/SmartStudyPal

---

本文档记录开发过程中向 AI 提交的关键提示词及对应的决策与结果。

---

## 一、项目初始化与架构搭建

### 1.1 后端 FastAPI 框架搭建

```
【用户意图】
按照项目计划书搭建后端服务框架，包含 API 路由、数据库、算法引擎层

【实现】
- FastAPI + uvicorn 异步服务器
- SQLite 本地数据库（DatabaseManager 自动建表）
- 模块化目录结构：api/、modules/（vision/voice/）、database.py
- RESTful 接口设计：/api/vision、/api/voice、/api/pomodoro、/api/dashboard、/api/health
- OpenAPI/Swagger 文档自动生成
```

### 1.2 视觉专注度检测模块开发

```
【用户意图】
实现基于 Dlib 人脸检测 + MediaPipe Pose 的专注度评分系统

【核心模块】
- FaceDetector: Dlib HOG+SVM 检测器 + 68点关键点预测 + EAR 计算
- FocusScorer: 多因子加权模型（眼部40% + 头部30% + 身体30%）+ 滑动窗口平滑
- PoseEstimator: MediaPipe Pose 33点 → 低头/趴桌/离座检测
- ImagePreprocessor: 帧采样(1fps) + 640x480缩放 + ROI裁剪

【关键参数】
EAR阈值: attentive=0.3, drowsy=0.2; 滑动窗口=10帧;
连续闭眼=3帧判定疲劳; 输出0-100分 + 四级状态标签
```

### 1.3 语音处理基础模块开发

```
【用户意图】
构建语音识别、唤醒词检测、情绪分类、VAD静音检测的基础能力

【模块清单】
- ASR Whisper: whisper-tiny 中文语音识别 + 指令解析
- WakeWord: "阿米娅"唤醒词匹配 + VAD前置过滤
- EmotionClassifier: Librosa MFCC特征 → 三类情绪（专注/烦躁/疲惫）
- VadDetector: 能量阈值+过零率双参数 VAD
- TTS Engine: pyTTS 离线合成（后升级为 MiMo VoiceClone）
```

### 1.4 多模态融合引擎与降级策略

```
【用户意图】
实现视觉与语音数据的加权融合，以及服务健康监控和自动降级机制

【FusionEngine】
- 默认权重: 视觉0.7 + 语音0.3
- 冲突检测: 分差 >30 时触发，采用视觉优先原则（0.8:0.2 / 0.85:0.15）
- 平滑算法: 指数加权 0.7*new + 0.3*old
- 趋势分析: improving/stable/declining 方向判断

【DegradationManager】
- 四级状态: FULL → DEGRADED → MINIMAL → OFFLINE
- 错误累积到阈值自动降级，连续成功N次逐级恢复
- 根据服务状态动态调整融合权重比例
```

### 1.5 前端 Vue3 桌面场景界面开发

```
【用户意图】
搭建沉浸式桌面自习场景的前端界面，替代 Element Plus 方案

【技术选型变更】
计划书原定 Element Plus UI库 → 实际采用自研桌面场景 CSS
原因: 更贴合"智能陪伴自习"的沉浸式体验需求

【已实现组件】
- App.vue: 主场景（桌面背景 + 阿米娅角色 + 功能入口 + 设置面板）
- FocusView.vue: 摄像头画面 + 专注度环形图 + 实时数据面板
- DashboardView.vue: 学习数据统计可视化
- PomodoroView.vue: 番茄钟计时界面
- HomeView.vue: 系统首页
- WhiteNoisePlayer.vue: 白噪音选择与播放
- API封装层: voice.js / vision.js / dashboard.js / pomodoro.js
- 路由配置: Vue Router 路由管理
```

---

## 二、阿米娅声线克隆（MiMo VoiceClone）

### 2.1 技术方案引入

```
【用户提示词】
阅读 mimo_voiceclone_trae_guide.md，用 MiMo-V2.5-TTS-VoiceClone 克隆阿米娅的声音

【决策】
pyTTS（原计划）→ MiMo V2.5 VoiceClone（实际）
理由: pyTTS无音色克隆能力；MiMo支持参考样本复刻且限时免费兼容OpenAI协议

【核心架构】
AmiyaTTS 类: 参考音频编码为Data URI → MiMo API调用(OpenAI协议) → Base64解码保存缓存
三级路由: 预录制关键词匹配 → MD5缓存命中 → 动态合成并缓存
```

### 2.2 问题排查与修复

| # | 问题 | 用户反馈 | 修复方案 |
|---|------|---------|---------|
| 1 | 点击无声音 | "听不到声音" | sounddevice服务端播放 → 改为前端 `new Audio(url).play()` |
| 2 | 播放预录制非合成 | "反馈的是欢迎回家不是博士我在" | 添加 force_synthesize 参数强制走合成路径 |
| 3 | 服务启动超时 | ERR_ABORTED /api/health | pre_synthesize() 改为 daemon Thread 后台执行 |
| 4 | API返回undefined | "请求失败" | axios拦截器已解包 response.data，前端代码多解了一层 |
| 5 | 合成声音沙哑颤音 | "沙哑，结尾有明显颤音" | 参考音频从2s问候.wav换为10.4s干员报到.wav |
| 6 | 语音拖沓 | "自然收束时间太长拖沓" | 风格指令改为"干脆利落不拖沓"，文本标签去掉"(自然收束)" |
| 7 | 语速问题 | "太快"/"慢0.2至0.3秒" | 风格指令迭代为"语速适中偏快" |
| 8 | 音高偏低 | "比录制里的音高低了" | 风格指令添加"音调自然偏高" |

### 2.3 风格指令调优过程

```
v1: 温柔亲切...语速适中偏慢...尾音自然收束 → 拖沓
v2: ...语速稍快...禁止颤抖沙哑拖音 → 结尾仍拖长
v3: 语速适中偏快，干脆利落 → 解决拖沓
v4 (最终): 角色=明日方舟阿米娅罗德岛领袖；风格=温柔亲切带责任感，
         清亮干净，音调自然偏高，语速适中偏快，干脆利落；
         基调=温暖关怀偶尔成熟担当；禁止=颤抖/沙哑/拖音/过度撒娇
```

### 2.4 参考音频迭代

```
问候.wav(2.0s) → 干员报到.wav(10.4s) → Reference.wav(用户自定义)
样本过短导致学习不充分 → 音质改善但仍有瑕疵 → 最终定稿
```

---

## 三、实时时钟组件

```
【用户提示词】
在应用界面水平居中竖直中偏上位置增加实时时间日期组件

【实现】
Vue ref响应式数据 + setInterval(1000)更新
中文格式: HH:mm:ss + YYYY年M月D日 星期X
CSS: absolute居中(top:35%) + 毛玻璃面板(blur16px) + 圆角24px
时间52px粗体深棕 + 日期16px灰棕 + tabular-nums等宽数字
onUnmounted清理定时器防内存泄漏
z-index:9（低于阿米娅10）
```

---

## 四、Git 版本控制与 GitHub 推送

### 4.1 .gitignore 关键规则

```
__pycache__/*.pyc, *.db, node_modules/, dist/, .env,
backend/models/*.task *.dat (>100MB),
backend/static/amiya_cache/*.wav (可重新生成), .idea/
```

### 4.2 Git 安装配置（Windows, D:\AiTool\Git\）

```
Editor: VSCode | Branch: main | PATH: command line + 3rd-party
SSH: bundled OpenSSH | HTTPS: native Windows Secure Channel
Line endings: checkout Windows-style commit Unix-style
Terminal: MinTTY | git pull: fast-forward or merge
Credential: Git Credential Manager | Enable file system caching ✅
```

### 4.3 推送结果

```
仓库: https://github.com/Yue230141875/SmartStudyPal | 分支: main
提交: feat: SmartStudyPal initial version - Amiya voice clone via MiMo API,
      focus detection, real-time clock
文件数: 60 files | 代码量: 8,753 insertions(+)
```

---

## 五、技术选型变更汇总

| 计划方案 | 实际方案 | 变更原因 |
|---------|---------|---------|
| pyTTS 离线语音合成 | MiMo V2.5 VoiceClone API | 声线克隆能力 + 音质 |
| Element Plus UI 库 | 自研桌面场景 CSS | 沉浸式体验 |
| edge-tts + librosa音频迁移 | 直接 MiMo 克隆 | 效果不理想 |
| 同步预合成 | 异步后台线程(daemon) | 启动超时6分钟 |
| sounddevice 服务端播放 | Audio 前端浏览器播放 | 浏览器端输出 |

---

## 七、专注检测模块深度优化（v2）

> 时间范围：2026-05-19 ~ 2026-05-21
> 涉及文件：`focus_scorer.py`, `__init__.py`(vision), `StudySession.vue`, `voice.py`(api), `amiya_tts.py`

### 7.1 鼓励语音功能

```
【用户提示词】
添加功能：合成几句阿米娅鼓励学习的语音。在开始自习中的任意自习模式点击"开启"时，随机播放

【实现】
- AmiyaTTS 类新增 6 条鼓励学习文本预合成列表
- 新增 /api/voice/amiya/encouragement 接口（GET），随机返回一条鼓励语音的 audio_url
- StudySession.vue 在 startTimer() 时调用 playEncouragement()
- 前端使用 new Audio(url).play() 浏览器原生播放
```

### 7.2 鼓励语音音量统一

```
【用户反馈】
这6条阿米娅鼓励学习语音，与点击/长按阿米娅的声音音量不一致

【问题】
鼓励语音 audio.volume 硬编码为 0.8，未读取设置面板的 amiya_volume

【修复】
const vol = parseInt(localStorage.getItem('amiya_volume') || '80')
audio.volume = vol / 100
```

### 7.3 专注判定门槛优化

```
【用户反馈】
我多次变换面部大小和表情都只能达到"轻度分心"，无法达到"专注"

【修复】
专注标签阈值从 75 分降至 65 分
pitch 角度优化：低头写字(-45°~10°)给100分，区分"低头学习"与"抬头走神"
```

### 7.4 大幅转头评分异常修复（迭代4轮）

**第1轮：大幅转头分数反而提高**

```
【用户反馈】
大幅度左右转头(60°以上)专注度分数反而提高，而不是被判定为"分心"

【根因】
侧脸时 MediaPipe/dlib 的 yaw 角度估计不准，导致 pitch/yaw 组合后评分偏高

【修复】
新增人脸水平位置偏移检测(_face_offset_penalty)作为转头补充信号
总分硬上限惩罚机制：偏移大时 total_score ≤ 45 或 ≤ 55
```

**第2轮：不同方向偏头分数不一致**

```
【用户反馈】
不同方向的偏头，专注度检测分数不一样

【根因】
人脸位置偏移以画面绝对中心(0.5)为基准
用户坐姿不在正中央时，左右偏移量计算不公

【修复】
新增动态基准位置机制(_update_baseline)：
- 前8帧记录人脸X坐标，取中位数作为个人化基准线(baseline_x)
- 之后每帧计算相对偏移量：offset = |face_center_x - baseline_x|
实现方向无关的转头检测
```

**第3轮：位置偏移误判正视/低头**

```
【用户反馈】
人脸正对屏幕/低头写字但是面部不在屏幕中心，会被判定为分心
截图数据：眼部80, 头部20, 总分60 → 轻度分心

【根因】
_face_offset_penalty 返回硬性分数(10或30)
_calc_head_score 中 if penalty ≤ 20: return 20 直接打到底！

【修复】
1. 移除 _face_offset_penalty 硬惩罚逻辑
2. 重构为 _face_offset_severity 返回级别("large"/"medium")
3. 改为软上限：large→≤55, medium→75（不再直接归零）
```

**第4轮：大幅侧头仍不对称 + 最终根治**

```
【用户反馈】（提供5张截图）
右侧脸×2: head=65/80, 总分83
左侧脸×2: head=47/20, 总分75/68
正视: head=80, 总分85

【根因分析 - 两层问题】
Layer1: 位置偏移信号仍在干扰(yaw<25°时仍触发)
       向左转时 baseline_x 导致相对偏移被高估
Layer2: yaw≥50° 时 return 20 硬惩罚（左侧yaw被高估到50+）
Layer3（核心）: dlib EAR方向性不对称！
       向左转→dlib仍能追踪眼睛→EAR≈0.30→eye_score=100 ❌虚高
       向右转→dlib眼睛关键点畸变→EAR≈0.15→eye_score=15 ✅正确

【最终修复方案 - 三刀齐下】
① 彻底移除位置偏移信号（_face_offset_severity 不再被调用）
   理由：人脸画面位置不可靠，受摄像头角度/距离/坐姿影响
   
② yaw≥50° 硬惩罚改为渐进式：
   if abs(yaw) >= 50: yaw_score = 20（参与加权而非return 20）
   
③ 新增 EAR 可靠性校正（关键创新）：
   当 yaw 表明大幅转头时，限制 eye_score 上限
   yaw_mag >= 50: eye_score = min(eye_score, 30)
   yaw_mag >= 35: eye_score = min(eye_score, 55)
   yaw_mag >= 25: eye_score = min(eye_score, 75)

【测试验证结果】
| 场景     | yaw | head_score | 对称性 |
|---------|-----|-----------|--------|
| 正视     | 0°  | 100       | —      |
| 小幅左右 | ±5° | 100/100   | ✅完全 |
| 大幅左右 | ±35°| 67.0/67.0 | ✅完全 |
| 极端左右 | ±55°| 52.0/52.0 | ✅完全 |
```

### 7.5 控制台报错修复

```
【用户反馈】
点击阿米娅按钮控制台反应 net::ERR_ABORTED http://localhost:5173/api/health

【原因】
前端请求发到 Vite 开发服务器(5173)而非后端(8000)
vite.config.js 已配置代理但未生效（需重启Vite）

【修复】
重启 Vite 开发服务器加载代理配置
验证：(Invoke-WebRequest -Uri "http://localhost:5173/api/health").StatusCode == 200
```

### 7.6 FocusScorer v2 最终架构

```
专注度评分流程:
┌─────────────────────────────────────────────┐
│ 输入: ear_left, ear_right, head_pose,        │
│       body_pose, face_rect, frame_shape      │
├─────────────────────────────────────────────┤
│ 1. 更新动态基准线 (_update_baseline)          │
│    前8帧人脸X坐标中位数 → baseline_x         │
│                                             │
│ 2. 计算各维度原始分                          │
│    eye_score  = _calc_eye_score(EAR)         │
│    head_score = _calc_head_score(pitch,yaw)  │
│    body_score = _calc_body_score(label)      │
│                                             │
│ 3. EAR可靠性校正（新增）                     │
│    if yaw幅度大: 限制eye_score上限           │
│    ≥50°→30, ≥35°→55, ≥25°→75               │
│                                             │
│ 4. 加权求和                                  │
│    total = eye*0.4 + head*0.3 + body*0.3    │
│                                             │
│ 5. 滑动窗口平滑                              │
│    smoothed = 0.5*total + 0.5*prev           │
│                                             │
│ 6. 标签映射                                  │
│    ≥65专注 / ≥45轻度分心 / ≥25明显走神 / 疲劳│
└─────────────────────────────────────────────┘

head_score 计算（纯角度驱动，零位置依赖）:
┌───────────────────┬────────────┬───────────┐
│ yaw 范围          │ yaw_score  │ 说明      │
├───────────────────┼────────────┼───────────┤
│ |yaw| < 20°       │ 100        │ 正视/微调 │
│ 20°~35°           │ 75         │ 轻度偏头  │
│ 35°~50°           │ 45         │ 大幅转头  │
│ ≥50°              │ 20         │ 极端侧脸  │
└───────────────────┴────────────┴───────────┘
× 0.6 + pitch_score × 0.4
```

---

## 六、待办事项

- [ ] 房间/座位预约功能开发
- [ ] 前后端全面联调测试
- [ ] AC-1 视觉模块准确率实测（目标≥85%）
- [ ] 多浏览器兼容性测试（Firefox）
- [ ] 连续30分钟稳定性压力测试
- [ ] MiMo API Key 安全管理（当前硬编码）

---

*生成于 2026-05-19 · 基于 Trae Vibe Coding 会话记录整理*
*更新于 2026-05-21 · 新增第七章：专注检测模块深度优化（v2）*
